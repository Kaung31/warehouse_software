{
  "$schema": "https://railway.app/railway.schema.json",
  "_comment": "Service config for the Trigger.dev worker. In Railway: create a SECOND service in the same project, point it at this file via the 'Custom Build Configuration' field, or just override the start command in the Railway UI to `npx trigger deploy && npx trigger dev`. Same env vars (DATABASE_URL, RESEND_API_KEY, etc) are shared via Railway 'Shared Variables'.",
  "build": {
    "builder":      "NIXPACKS",
    "buildCommand": "npm ci && npx prisma generate"
  },
  "deploy": {
    "startCommand":         "npx trigger dev",
    "healthcheckPath":      "/",
    "restartPolicyType":    "ON_FAILURE",
    "restartPolicyMaxRetries": 5,
    "numReplicas":          1,
    "region":               "europe-west2"
  }
}
